

import static spark.Spark.*;

public class test_webservice {
    public static void main(String[] args) {
    	BasicConfigurator.configure();
    	port(8666);
        get("/predict", (req, res) -> "Hello World");
    }
}